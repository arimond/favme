{
  "name": "backend",
  "version": "1.0.0",
  "description": "FavMe Backend",
  "main": "server.js",
  "scripts": {
    "test": "npm run createTestDatabase && NODE_ENV=test mocha --recursive",
    "createTestDatabase": "mysql -h localhost -u root -p < scripts/createTestDatabase.sql",
    "start": "NODE_ENV=development nodemon server.js"
  },
  "author": "Peer Arimond",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "nodemon": "^2.0.4"
  }
}
